@model IEnumerable<Aplicacion.Models.TipoActivo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cuentascontablecompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cuentacontabledepreciacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cuentascontablecompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cuentacontabledepreciacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Activo }) |
            @Html.ActionLink("Details", "Details", new { id=item.Activo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Activo })
        </td>
    </tr>
}

</table>
